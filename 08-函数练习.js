/* ============================================================
   学习日 4 · 独立练习：函数
   ------------------------------------------------------------
   规则不变：
   1. 写在每题 "// TODO:" 下面，Ctrl + S 保存
   2. 每题都用 "1)" "2)" 这样开头打印（批改脚本靠编号认题）
   3. 写完运行：node 检查.js
   ------------------------------------------------------------
   这次的练习不一样：以前是一口气写完拉倒，
   这次要把每段逻辑"包进函数里"，让它可以反复用。
   ============================================================ */


// ===== 练习 1：求和函数 =====
// 要求：写一个普通函数 sumTo(n)，返回 1 到 n 的和（用 for 循环算，别用公式）
//       然后调用 sumTo(100)，打印一行，以 "1)" 开头
// 期望看到类似：1) 1 到 100 的和是 5050
// 提示：函数里最后要有 return，把算出来的结果交出去

// TODO: 在这里写你的代码
function sumTo(n){
   let sum = 0 ;
   for(let i =1;i <=n;i++){
      sum =sum +i;
   }
   return sum ;
}
let n = 100;
console.log("1) 1到",n,"的和是",sumTo(n));


// ===== 练习 2：判断最大值的函数 =====
// 要求：写一个函数 maxOf(a, b)，返回两个数里较大的那个
//       调用两次：maxOf(13, 27) 和 maxOf(99, 5)
//       每次调用都打印一行，两行都以 "2)" 开头
// 期望看到类似：2) 13 和 27 里较大的是 27
//                2) 99 和 5 里较大的是 99
// 提示：函数里用 if 判断，哪个大就 return 哪个

// TODO: 在这里写你的代码
function maxOf(a,b){
   if(a>b)
      return a;
   else return b;
   }
let a = 13,b=27;
   console.log("2)",a,"和",b ,"中较大的是",maxOf(a,b));
a = 99,b=5;
   console.log("2)",a,"和",b ,"中较大的是",maxOf(a,b));

// ===== 练习 3：字符串反转函数（用箭头函数写）=====
// 要求：用箭头函数写 reverseText(s)，返回反转后的字符串
//       调用两次：reverseText("深圳前端") 和 reverseText("abc")
//       每次调用都打印一行，两行都以 "3)" 开头
// 期望看到类似：3) 反转后是：端前圳深
//                3) 反转后是：cba
// 提示：循环那段逻辑就是学习日 3 第 8 题，把它搬进函数
//       箭头函数带 return 的写法：
//       const 函数名 = (参数) => {
//         ...要几行写几行...
//         return 结果;
//       };

// TODO: 在这里写你的代码
const reverseText = (s) =>{
   let result="";
   for(let i =s.length-1;i>=0;i--){
      result = result + s[i]
   }
   return result;
}
console.log("3)反转之后是",reverseText("深圳前端"));
console.log("3)反转之后是",reverseText("abc"));

/* ===== 写完之后 =====
   1. 终端运行：node 检查.js
   2. 结果发我
*/
